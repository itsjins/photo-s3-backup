# Filesystem paths
IMMICH_VOLUME_PATH="/Volumes/One Touch"
IMMICH_UPLOAD_PATH="/Volumes/One Touch/immich"
IMMICH_COMPOSE_DIR="/Users/songjin/docker/immich"
POSTGRES_CONTAINER="immich_postgres"

# AWS CLI configuration. Credentials stay in this named profile.
AWS_PROFILE="immich-backup"
S3_BUCKET="jin-immich-backup"

# Relative paths are resolved from the directory containing backup.sh.
LOG_DIR="./logs"
